'use client'; import {useEffect,useState} from 'react'; import Link from 'next/link';
export default function NeighborhoodAdmin(){const [ns,setNs]=useState<any[]>([]),[form,setForm]=useState({name:'',slug:'',city:'الرياض',description:''});async function load(){const r=await fetch('/api/admin/neighborhoods');if(r.status===401)return location.href='/admin';setNs(await r.json())}useEffect(()=>{load()},[]);async function add(e:any){e.preventDefault();await fetch('/api/admin/neighborhoods',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});setForm({name:'',slug:'',city:'الرياض',description:''});load()}async function del(id:string){if(confirm('حذف الحي؟')){const r=await fetch('/api/admin/neighborhoods/'+id,{method:'DELETE'});if(!r.ok)alert((await r.json()).error);load()}}return <main className="section"><div className="container"><div className="sectionhead"><div><h1>إدارة الأحياء</h1><p className="muted">إضافة وحذف الأحياء.</p></div><Link className="btn" href="/admin/properties">العودة للعقارات</Link></div><div className="panel"><form onSubmit={add} className="formgrid"><input required className="field" placeholder="اسم الحي" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/><input className="field" placeholder="Slug اختياري" value={form.slug} onChange={e=>setForm({...form,slug:e.target.value})}/><input className="field" placeholder="المدينة" value={form.city} onChange={e=>setForm({...form,city:e.target.value})}/><input className="field" placeholder="وصف اختياري" value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/><button className="btn primary">+ إضافة حي</button></form></div><div className="panel" style={{marginTop:18}}><table className="table"><thead><tr><th>الحي</th><th>المدينة</th><th>العقارات</th><th>إجراء</th></tr></thead><tbody>{ns.map(n=><tr key={n.id}><td>{n.name}</td><td>{n.city}</td><td>{n._count.properties}</td><td><button className="btn" onClick={()=>del(n.id)}>حذف</button></td></tr>)}</tbody></table></div></div></main>}
