import {notFound} from 'next/navigation'; import {prisma} from '@/lib/prisma'; import AdminPropertyForm from '@/components/AdminPropertyForm';
export default async function Edit({params}:{params:{id:string}}){const [p,ns]=await Promise.all([prisma.property.findUnique({where:{id:params.id},include:{images:{orderBy:{sortOrder:'asc'}}}}),prisma.neighborhood.findMany({orderBy:{name:'asc'}})]);if(!p)return notFound();return <main className="section"><div className="container"><div className="panel"><h1>تعديل العقار</h1><AdminPropertyForm initial={p} neighborhoods={ns}/></div></div></main>}
