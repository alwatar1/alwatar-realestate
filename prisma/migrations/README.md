# PostgreSQL migration

This migration is generated for the PostgreSQL Prisma datasource. It creates the complete schema used by the application, including admins, neighborhoods, properties, property images, and site settings.
